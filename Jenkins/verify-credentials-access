pipeline {
  agent any

  stages {
    stage('Test Credentials') {
      steps {
        withCredentials([usernamePassword(credentialsId: 'version-tracking', usernameVariable: 'USER', passwordVariable: 'TOKEN')]) {
          sh 'echo "Found credentials for user: $USER"'
        }
      }
    }
  }
}
